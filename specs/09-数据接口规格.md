# 09 — 数据接口规格

| 项 | 值 |
|---|---|
| 模块编号 | M1-MuseumGuide |
| 模块名称 | 博物馆漫步 WebApp |
| 文档版本 | v1.1 |
| 文档状态 | Approved |

---

> **说明**: 本项目为纯前端静态应用，无后端 API。本文档定义前端数据层的接口契约，即 `src/data/index.ts` 导出的查询函数和数据导出。

## 1. 数据层接口总览

| 接口 | 类型 | 返回值 | 说明 |
|------|------|--------|------|
| `museums` | 常量导出 | `Museum[]` | 全部博物馆数据数组 |
| `getMuseumById(id)` | 函数 | `Museum \| undefined` | 按 ID 查询单个博物馆 |
| `getExhibitById(museumId, exhibitId)` | 函数 | `Exhibit \| undefined` | 按博物馆 ID 和展品 ID 查询展品 |

## 2. 接口详细规格

### 2.1 museums 常量

```typescript
export const museums: Museum[]
```

**数据来源**: 静态导入自 `museums/forbidden-city.ts`、`museums/shaanxi-history.ts`、`museums/sanxingdui.ts`、`museums/uffizi.ts`、`museums/british-museum.ts`

**数据保证**:
- MUST 包含 5 个博物馆对象
- 每个博物馆 MUST 有唯一 `id` 字段
- `id` 值: `"forbidden-city"` | `"shaanxi-history"` | `"sanxingdui"` | `"uffizi"` | `"british-museum"`

**示例**:
```typescript
[
  { id: "forbidden-city", name: "故宫博物院", ... },
  { id: "shaanxi-history", name: "陕西历史博物馆", ... },
  { id: "sanxingdui", name: "三星堆博物馆", ... },
  { id: "uffizi", name: "乌菲齐美术馆", ... },
  { id: "british-museum", name: "大英博物馆", ... }
]
```

### 2.2 getMuseumById

```typescript
export function getMuseumById(id: string): Museum | undefined
```

| 参数 | 类型 | 必填 | 约束 |
|------|------|------|------|
| id | string | 是 | 博物馆 ID |

**行为规格**:
- Given 有效 ID（如 `"forbidden-city"`），MUST 返回对应 `Museum` 对象
- Given 无效 ID（如 `"louvre"`），MUST 返回 `undefined`
- 查找方式: `museums.find(m => m.id === id)`

**示例**:
```typescript
getMuseumById("uffizi")
// → { id: "uffizi", name: "乌菲齐美术馆", ... }

getMuseumById("nonexistent")
// → undefined
```

### 2.3 getExhibitById

```typescript
export function getExhibitById(museumId: string, exhibitId: string): Exhibit | undefined
```

| 参数 | 类型 | 必填 | 约束 |
|------|------|------|------|
| museumId | string | 是 | 博物馆 ID |
| exhibitId | string | 是 | 展品 ID |

**行为规格**:
- 先通过 `getMuseumById` 查找博物馆
- 在 `highlights` 和 `hiddenGems` 中搜索展品
- Given 有效的博物馆 ID 和展品 ID，MUST 返回对应 `Exhibit` 对象
- Given 无效的博物馆 ID 或展品 ID，MUST 返回 `undefined`

**示例**:
```typescript
getExhibitById("forbidden-city", "qingming-scroll")
// → { id: "qingming-scroll", name: "清明上河图", ... }

getExhibitById("forbidden-city", "nonexistent")
// → undefined
```

## 3. Context 接口

### 3.1 GuidePreferenceContext

```typescript
type GuideMode = 'popular' | 'enthusiast';

interface GuidePreferenceContextType {
  mode: GuideMode;
  setMode: (mode: GuideMode) => void;
}
```

**Provider**: `GuidePreferenceProvider`

**持久化契约**:
- MUST 在 `setMode` 调用时写入 `localStorage`
- Key: `"museum-guide-mode"`
- Value: `"popular"` | `"enthusiast"`
- MUST 在初始化时读取 localStorage，若无记录则默认 `"popular"`

**使用方式**:
```typescript
const { mode, setMode } = useGuidePreference();
```

## 4. Hook 接口

### 4.1 useSnapScroll

```typescript
function useSnapScroll(totalPages: number): {
  containerRef: React.RefObject<HTMLDivElement>;
  currentPage: number;
  scrollToPage: (index: number) => void;
}
```

| 参数 | 类型 | 必填 | 约束 |
|------|------|------|------|
| totalPages | number | 是 | 总页数，MUST > 0 |

**返回值契约**:
- `containerRef`: MUST 绑定到 `.snap-container` 元素
- `currentPage`: 0-based 当前可见页码，MUST 在 `[0, totalPages-1]` 范围内
- `scrollToPage(index)`: 调用后 MUST 平滑滚动到指定页面

**实现机制**: Scroll 事件监听 + `requestAnimationFrame` 节流
- 计算 `scrollTop / clientHeight` 取整得到当前页码
- 滚动到底部时强制显示最后一页
- RAF 节流状态更新，减少渲染压力

### 4.2 useNestedScroll

```typescript
function useNestedScroll(): {
  ref: React.RefObject<HTMLElement>;
  onTouchStart: (e: React.TouchEvent) => void;
  onTouchMove: (e: React.TouchEvent) => void;
  onWheel: (e: React.WheelEvent) => void;
}
```

**用途**: 处理嵌套在 snap-page 内的可滚动区域的事件传播
- 阻止触摸滑动在内部滚动到达边界时触发外层 snap 滚动
- 阻止滚轮事件冒泡到外层 snap 容器

### 4.3 useInView

```typescript
function useInView<T extends HTMLElement>(margin = '-80px'): {
  ref: React.RefObject<T>;
  isInView: boolean;
}
```

**用途**: IntersectionObserver 封装，用于首页卡片入场动画等场景
- `margin`: 可选的根边距，默认 `'-80px'`
- 元素进入视口时 `isInView` 变为 `true`
- 组件卸载时自动 disconnect

## 5. 数据量规格

| 博物馆 | 代表展品 | 冷门展品 | 推荐路线 | 楼层 |
|--------|---------|---------|---------|------|
| 故宫博物院 | 11 件 | 4 件 | 3 条 | 5 区 |
| 陕西历史博物馆 | 12 件 | 4 件 | 3 条 | — |
| 三星堆博物馆 | 12 件 | 4 件 | 3 条 | — |
| 乌菲齐美术馆 | 12 件 | 4 件 | 3 条 | 4 区 |
| 大英博物馆 | 12 件 | 4 件 | 3 条 | 4 区 |
| **合计** | **59 件** | **20 件** | **15 条** | **13 区** |

## 6. 数据文件映射

| 数据文件 | 导出 | 类型 |
|---------|------|------|
| `data/museums/forbidden-city.ts` | `forbiddenCity` | `Museum` |
| `data/museums/shaanxi-history.ts` | `shaanxiHistory` | `Museum` |
| `data/museums/sanxingdui.ts` | `sanxingdui` | `Museum` |
| `data/museums/uffizi.ts` | `uffizi` | `Museum` |
| `data/museums/british-museum.ts` | `britishMuseum` | `Museum` |
| `data/types.ts` | 全部接口定义 | TypeScript interfaces |
| `data/index.ts` | `museums`, `getMuseumById`, `getExhibitById` | 查询入口 |
